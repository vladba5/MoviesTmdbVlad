package com.example.moviestmdb.core.util

import android.content.Context
import java.util.*

class SharedPref {
//    companion object{
//        fun saveData(context: Context) {
//            val preference= context.getSharedPreferences("shared preferences", Context.MODE_PRIVATE) ?: return
//            with(preference.edit()) {
//                putString("time", Calendar.getInstance().timeInMillis.toString())
//                apply()
//            }
//        }
//
//        fun getData(context: Context) : String? {
//            val sharedPref = context.getSharedPreferences("shared preferences", Context.MODE_PRIVATE) ?: return
//            val lastDate = sharedPref.getString("time", "")
//
//            return lastDate
//        }
//    }
}